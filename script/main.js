// 画面の読み込み時
$(function() {
    // script
});

//画面が読み込まれた後
$(window).on('load',function() {
    
    // script
});

// スクロール時
$(window).on("scroll",function() {
    // script
})

//画面がリサイズされたら
$(window).on('resize', function () {
    // script
});